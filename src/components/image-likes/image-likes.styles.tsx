import styled, { css } from 'styled-components';

export const LikedByContainer = styled.div`
  display: flex;
  padding: 10px;
`;

export const LikedByUsersList = styled.div`
  font-weight: bold;
  cursor: pointer;
`;